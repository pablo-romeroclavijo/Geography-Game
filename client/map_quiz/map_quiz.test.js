const each = require('jest-each').default;
const { checkAnwser} = require("./map_quiz.js");

describe("checkAnswer", () => {

    it("checkAnswer", () => {
        expect(checkAnwser).toBeDefined();
    })
    

});