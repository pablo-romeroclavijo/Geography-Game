const each = require('jest-each').default;
const { submituser} = require("./map_quiz.js");

describe("submituser", () => {

    it("submituser", () => {
        expect(submituser).toBeDefined();
    })
    

});